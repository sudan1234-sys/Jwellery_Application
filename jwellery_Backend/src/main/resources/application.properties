spring.application.name=jwellery

server.port=${PORT:8080}

# --- Database Connection (Neon Postgres) ---
spring.datasource.url=jdbc:postgresql://ep-cold-recipe-adzkr32h-pooler.c-2.us-east-1.aws.neon.tech/neondb?user=neondb_owner&password=npg_6wO8TqdZshSn&sslmode=require&channelBinding=require
spring.datasource.username=neondb_owner
spring.datasource.password=npg_6wO8TqdZshSn

# --- Connection Pooling (HikariCP, default in Spring Boot) ---
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.idle-timeout=30000
spring.datasource.hikari.connection-timeout=20000

# --- JPA / Hibernate Settings ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# --- Flyway Migration (for versioned schema updates) ---
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
spring.security.user.name=admin
spring.security.user.password=admin123